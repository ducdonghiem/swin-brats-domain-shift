training:
  epochs: 100
  warmup_epochs: 5
  learning_rate: 0.001
  checkpoint_dir: "checkpoints"
  log_interval: 10
  batch_size: 1
  num_workers: 0

device: "cuda"  # or "cpu"

model:
  in_channels: 155
  num_classes: 4
  embed_dim: 32
  window_size: 7
  patch_size: 4

# TODO: Early stopping criteria
early_stopping:
